export const input = `

    input CreatePageHeaders {
      key: String!
      value: String!
    }

    input ScriptMetaInput {
      skipContentEnabled: Boolean
      translateEnabled: Boolean
    }

`;
