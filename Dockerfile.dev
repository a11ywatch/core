FROM node:14.18.2-alpine

WORKDIR /usr/src/app

COPY package*.json bootstrap.sh ./

RUN apk upgrade --update-cache --available && \
	apk add openssl python3 make gcc g++ libc-dev && \
	rm -rf /var/cache/apk/*

RUN ./bootstrap.sh
RUN npm ci

COPY . .

CMD [ "npm", "run", "dev" ]
